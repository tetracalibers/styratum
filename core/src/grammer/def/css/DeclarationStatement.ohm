DeclarationStatement <: RuleSet {
  pureAtomic += else | invert
  addedAtomic += truthy | falsy | exist
  
  /* -------------------------------------------------------------------------- */
  /* declaration statement                                                      */
  /* -------------------------------------------------------------------------- */
  
  DeclarationStatement = 
    | RuleSetWithPrefix RuleSet* sufAnnotation -- surrounded
    | RuleSetWithPrefix midAnnotation RuleSet -- between
    | RuleSetWithPrefix -- prefixed
    | RuleSet -- basic
  RuleSetWithPrefix = preAnnotation RuleSet
  
  /* -------------------------------------------------------------------------- */
  /* annotation                                                                 */
  /* -------------------------------------------------------------------------- */
  preAnnotation = truthy | falsy | exist
  midAnnotation = else
  sufAnnotation = invert
    
  truthy = "@" "truthy" "(" jsIdentifier ")"
  falsy = "@" "falsy" "(" jsIdentifier")"
  exist = "@" "exist" "(" jsIdentifier ")"
  else = "@" "else"
  invert = "@" "invert"
}